# Experimental-UnitChart

# Development
Update `deploy/fb_dev_constants.json`
Run `bash deploy/dev.sh`
Open `http://localhost:8080` in browser
Browser reloads on file changes

# Production
`To be filled`
